[project]
name = "email-detection-service"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]",
    "numpy",
    "onnxruntime",
    "pydantic",
    "transformers",
]
